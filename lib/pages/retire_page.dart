import 'package:flutter/material.dart';

class RetirePage extends StatelessWidget {
  const RetirePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Retire Page")),
      body: const Center(child: Text("This is a new page")),
    );
  }
}